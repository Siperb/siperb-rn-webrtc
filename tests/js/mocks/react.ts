/** Enough of React for RTCPIPView.tsx to load; no component is ever rendered in these tests. */
export class Component {}

export function forwardRef(render: any) {
    return render;
}

export default { Component, forwardRef };
